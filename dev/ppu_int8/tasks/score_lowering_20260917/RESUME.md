# Resume

Screen complete; production parent 41f68d5 and published binaries unchanged.
Unconditional FMA rejected by a constant-V witness beyond the existing O
tolerance. Global INT8 V scale passes 16 random cases but fails causal AND
noncausal unattended-outlier witnesses (relative error 1); not adopted.

Biased QK representation exhausts 4194305 integers exactly. Actual SDK probe
298->273 native instructions, regs62/80 unchanged and zero stack. This changes
scaled-logit rounding and is not a full attention implementation/admission.
Do not transfer its 25-instruction reduction onto the published H3 kernel.

Normal-event INT8/FP16 core comparison is now in the execution-only runner.
CPU discrimination tests pass, but device timing is NOT RUN. Same DSO contains
both paths; original inputs and quantized Q/K shared, preparation excluded,
alternating arms. 570 ms still has no bound SHA/report or timer identity.
See docs/PPU_SCORE_LOWERING_SCREEN.md for witnesses, commands and next gates.
